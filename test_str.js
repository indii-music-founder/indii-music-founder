import { FunctionsError } from '@firebase/functions';
// We don't have the module easily importable, but we know FirebaseError exists.
console.log("I am testing string matching");
console.log('INTERNAL' !== 'internal');
