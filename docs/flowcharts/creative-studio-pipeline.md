# Creative Studio Image Generation and Annotation Flowchart

This diagram maps the current image-generation and spatial-annotation paths. It distinguishes the direct `generateImageV3` callable from the inline annotation adapter that ultimately invokes the separate `editImage` callable.

```mermaid
graph TD
    subgraph UserUI ["User and React UI"]
        PromptUI["Creative generation prompt"]
        GeneratedImage["Generated chat image"]
        Annotator["ImageAnnotator circle editor"]
        PromptGate{"Instruction for every used color?"}
        ErrorAlert["Inline actionable error"]
        ResultUI["ToolOutputRenderer edited image"]
    end

    subgraph Renderer ["Renderer services"]
        GenerationService["ImageGenerationService"]
        AgentDispatch["AgentService dispatchToolCall"]
        ToolRegistry["TOOL_REGISTRY"]
        AnnotationTool["EditImageWithAnnotationsTool"]
        PayloadGate{"Source, mask, geometry, and prompt valid?"}
        EditingService["EditingService"]
        StorageAdapter["CreativeStorageService"]
    end

    subgraph Firebase ["Firebase callable boundary"]
        GenerateFn["generateImageV3"]
        EditFn["editImage"]
        Admission["Auth, App Check, Arcjet, entitlement, and cost gates"]
        ImageConfig["Model capability and location validation"]
    end

    subgraph GoogleCloud ["Google Cloud"]
        VertexGlobal["Vertex AI global endpoint"]
        FlashModel["gemini-3.1-flash-image"]
        ProModel["gemini-3-pro-image"]
        Storage["Owner-scoped Cloud Storage objects"]
        Metadata["Creative job and asset metadata"]
    end

    PromptUI -->|"Prompt, settings, owned reference URIs"| GenerationService
    GenerationService -->|"Authenticated callable request"| GenerateFn
    GenerateFn --> Admission
    Admission --> ImageConfig
    ImageConfig -->|"Fast"| FlashModel
    ImageConfig -->|"Pro or grounded"| ProModel
    FlashModel --> VertexGlobal
    ProModel --> VertexGlobal
    VertexGlobal -->|"Generated bytes"| GenerateFn
    GenerateFn -->|"Persist output"| Storage
    GenerateFn -->|"Write job receipt"| Metadata
    Storage -->|"Owned result URI"| GeneratedImage

    GeneratedImage --> Annotator
    Annotator -->|"Circles plus color instructions"| PromptGate
    PromptGate -->|"No"| ErrorAlert
    PromptGate -->|"Yes: binary PNG mask"| AgentDispatch
    AgentDispatch --> ToolRegistry
    ToolRegistry --> AnnotationTool
    AnnotationTool --> PayloadGate
    PayloadGate -->|"Invalid"| AgentDispatch
    AgentDispatch -->|"Rejected promise"| ErrorAlert
    PayloadGate -->|"Valid source and PNG mask"| EditingService
    EditingService -->|"Upload source and mask once"| StorageAdapter
    StorageAdapter --> Storage
    EditingService -->|"Owned imageUri, maskUri, and prompt"| EditFn
    EditFn --> Admission
    Admission --> ImageConfig
    ImageConfig --> ProModel
    VertexGlobal -->|"Edited bytes"| EditFn
    EditFn -->|"Edited result"| EditingService
    EditingService --> AnnotationTool
    AnnotationTool -->|"Result URL"| AgentDispatch
    AgentDispatch --> ResultUI

    style PromptUI fill:#00D4FF,color:#000,stroke:#007C91
    style GeneratedImage fill:#00D4FF,color:#000,stroke:#007C91
    style Annotator fill:#00D4FF,color:#000,stroke:#007C91
    style ResultUI fill:#00D4FF,color:#000,stroke:#007C91
    style GenerationService fill:#8A2BE2,color:#FFF,stroke:#4B0082
    style AgentDispatch fill:#8A2BE2,color:#FFF,stroke:#4B0082
    style AnnotationTool fill:#8A2BE2,color:#FFF,stroke:#4B0082
    style EditingService fill:#8A2BE2,color:#FFF,stroke:#4B0082
    style GenerateFn fill:#FF8C00,color:#000,stroke:#9A5200
    style EditFn fill:#FF8C00,color:#000,stroke:#9A5200
    style Admission fill:#FF00FF,color:#000,stroke:#8B008B
    style PromptGate fill:#FF00FF,color:#000,stroke:#8B008B
    style PayloadGate fill:#FF00FF,color:#000,stroke:#8B008B
    style ErrorAlert fill:#FF6B81,color:#000,stroke:#A00020
    style VertexGlobal fill:#39FF14,color:#000,stroke:#168000
    style Storage fill:#39FF14,color:#000,stroke:#168000
    style Metadata fill:#39FF14,color:#000,stroke:#168000
```

## Transition Breakdown

1. **Direct generation:** `ImageGenerationService` sends the signed-in user's prompt, model tier, aspect ratio, and owned reference URIs to `generateImageV3`. Browser API keys are never part of the payload.
2. **Admission and routing:** The callable applies authentication, App Check, abuse protection, entitlement, and cost controls before model execution. Image requests use the Vertex AI global endpoint. Fast routes to `gemini-3.1-flash-image`; Pro and supported Google Search grounding route to `gemini-3-pro-image`.
3. **Persistence:** The backend writes generated bytes to owner-scoped Cloud Storage and records the corresponding creative job or asset metadata. The renderer receives the owned result URI and renders the generated image.
4. **Annotation capture:** `ImageAnnotator` records red, blue, and yellow circles in natural-image coordinates. Every used color must have a non-empty instruction. The UI converts the union of those regions into a black-and-white PNG mask.
5. **Tool validation:** `AgentService` calls the registered `edit_image_with_annotations` tool. `EditImageWithAnnotationsTool` rejects missing sources, non-HTTPS remote sources, non-PNG masks, unsupported colors, invalid geometry, excessive annotations, and missing or oversized instructions before provider work begins.
6. **Secured edit:** `EditingService` uploads the source and mask through `CreativeStorageService`, then calls `editImage` with owned `gs://` URIs. The edit callable applies the same server-side admission boundary and executes the Pro image model on the global endpoint.
7. **Success and recovery:** A successful tool result is surfaced through `ToolOutputRenderer`. A tool error is converted into a rejected direct call, preserved in the agent system history, and shown as an inline alert while the user's circles and instructions remain available for retry.
