# Social Media Namespace Allocation Strategy

**Brand:** indiiOS / indii.music
**Date:** May 10, 2026

As part of Phase 5 of the provisioning plan, securing consistent namespace across all major social networks is critical for brand integrity and SEO visibility.

## 1. Naming Conventions

The primary target is the exact match of the domain: **`indii.music`**. 
Since some platforms do not support periods (`.`) in usernames, the official fallback is **`indiimusic`**.

* **Primary:** `indii.music` (Used where allowed: TikTok, Instagram, YouTube)
* **Secondary:** `indiimusic` (Used where periods are stripped: X/Twitter, Discord)
* **Tertiary:** `indii_music` (Only if the secondary is taken and underscores are required)

---

## 2. Tier 1 Platforms (Priority: Critical)

These platforms represent the primary acquisition and engagement channels for the independent artist demographic.

### Instagram
* **Target Handle:** `@indii.music` or `@indiimusic`
* **Status:** Action Required
* **Action Item:** Log in to the current `@thewalkingagency` or `@indiios` account and initiate a handle change. Meta sometimes locks previous handles for 14 days; ensure the old handle is retained on a burner account if you wish to prevent squatting.

### TikTok
* **Target Handle:** `@indii.music`
* **Status:** Action Required
* **Action Item:** TikTok supports periods. Secure `@indii.music` immediately. Ensure the bio links directly to `https://indii.music`.

### YouTube
* **Target Handle:** `@indii.music`
* **Status:** Action Required
* **Action Item:** YouTube handles (aliases) support periods. Navigate to YouTube Studio -> Customization -> Basic Info to update the handle to `@indii.music`.

---

## 3. Tier 2 Platforms (Priority: High)

These platforms serve community-building, real-time updates, and technical support.

### X (Twitter)
* **Target Handle:** `@indiimusic` (X does not support periods in handles)
* **Status:** Action Required
* **Action Item:** Migrate the existing X account to `@indiimusic`. If taken, use `@indii_music` or `@indiiOS`. Update the display name to `indii.music`.

### Discord
* **Target Handle:** `discord.gg/indiimusic` (Vanity URL)
* **Status:** Action Required
* **Action Item:** Once the server reaches Server Boost Level 3 (or if utilizing a Partnered/Verified server), set the vanity URL to `indiimusic`. Ensure the server name is updated to `indii.music`.

### Threads
* **Target Handle:** `@indii.music`
* **Status:** Tied to Instagram
* **Action Item:** This will automatically sync when the Instagram handle is updated.

---

## 4. Standardization Guidelines

To ensure a unified brand presence across all namespaces, the following assets and configurations must be applied to every profile:

1. **Profile Avatar:** Use the official, high-resolution vector logo (`indiiOS-Clean/docs/assets/logo.svg` equivalent). The background should be `#0A0A0B` (the app's primary dark theme) to maintain consistency with the Admin Dashboard and Studio UI.
2. **Bio / Description:** *"The AI-native music business platform for independent music artists."*
3. **Primary Link:** `https://indii.music` (Ensure SSL is fully provisioned before publishing the link).
4. **Link-in-Bio:** Avoid third-party tools like Linktree if possible; instead, use an `indii.music/links` routed page built into the Next.js landing page to preserve domain authority.

---

## 5. Next Steps for Execution

1. [ ] Manually verify handle availability across the 5 platforms listed above.
2. [ ] Execute the handle changes on the existing corporate accounts.
3. [ ] If any handle is squatted, immediately file an Impersonation/Trademark claim with the respective platform using the `admin@indii.music` email once MX records propagate.
