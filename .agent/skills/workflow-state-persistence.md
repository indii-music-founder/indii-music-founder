# Workflow State Persistence
Defines the JSON state schema for the loop iterations, checkpoint structures, and the logic required to suspend and resume the agent's execution context without data loss.
