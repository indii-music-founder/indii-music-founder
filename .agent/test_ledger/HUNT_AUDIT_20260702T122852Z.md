# Hunter Audit 20260702T122852Z

Mode: AUDIT
Scope: `.agent/workflows/hunter.md`
Fix policy: find-only; no product code changes.
Ledger target: `.agent/test_ledger/OPEN_ISSUES.md` is the source of truth for all findings; this audit file keeps supporting evidence only.

## Big Game Findings

### ISSUE-652 | OPEN | MEDIUM | Email Provider Mutations

- **Module:** Renderer / email providers
- **Evidence:** `packages/renderer/src/services/email/GmailProvider.ts:294-328` and `packages/renderer/src/services/email/OutlookProvider.ts:225-261` call Gmail/Graph mutation endpoints for `markAsRead`, `toggleStar`, and `trashMessage`, but never inspect `response.ok`.
- **Evidence:** `packages/renderer/src/services/email/EmailService.ts:317-339` exposes these methods as successful `Promise<void>` calls.
- **Evidence:** `packages/renderer/src/modules/agent/components/InboxTab.tsx:533-555` depends on rejection to revert star state or show trash failure, but HTTP 4xx/5xx responses from the providers currently resolve.
- **Impact:** Users can see messages marked read, starred, or trashed locally while the provider rejected the operation. The UI can show success for a failed destructive mail operation.
- **Fix Direction:** Capture each mutation response, read/log the error body, and throw on non-2xx statuses. Add focused provider tests for Gmail and Outlook mutation failure paths and an `InboxTab` optimistic-revert test for star/trash failures.
- **Verified?:** Static verified by source read; runtime provider calls not executed.

### ISSUE-653 | OPEN | LOW | E2E Mode Console Noise

- **Module:** Renderer / test harness mode guard
- **Evidence:** `packages/renderer/src/utils/e2eMode.ts:32-58` uses `console.log` in `isFirebaseE2EMockEnabled()` for normal disabled/enabled branches. The first branch logs on normal production calls when the runtime is neither a test harness nor local dev host.
- **Impact:** Production users can get repeated harness diagnostic logs in the browser console. It is low severity, but it violates the hunter workflow's raw-console rule and makes real console diagnostics noisier.
- **Fix Direction:** Replace branch `console.log` calls with the repo logger at debug level behind a dev/test guard, or remove the logs entirely. Keep warning/error reporting only for actual exceptional reads.
- **Verified?:** Static verified by source read; no runtime console capture performed.

### ISSUE-655 | OPEN | HIGH | Rights Credentials Used Directly In Renderer

- **Module:** Renderer / rights / PRO integrations
- **GitHub:** https://github.com/indii-music-founder/indii-music-founder/issues/214
- **Evidence:** `packages/renderer/src/services/rights/PRORightsService.ts:66-69` reads ASCAP API credentials from Firestore in the renderer and `:118-127` posts directly to `https://api.ascap.com/v1/works/register` with `Authorization: Bearer ${creds.apiKey}`.
- **Evidence:** `packages/renderer/src/services/rights/PRORightsService.ts:187-218` reads BMI username/password into the renderer and posts directly to `https://worksexpress.bmi.com/api/auth`; `:228-249` posts BMI work registration from the renderer.
- **Evidence:** `packages/renderer/src/services/rights/PRORightsService.ts:292-329` reads SoundExchange credentials into the renderer and posts directly to `https://api.soundexchange.com/v1/enrollments`.
- **Evidence:** `packages/renderer/src/services/rights/PRORightsService.ts:402-408` sends Music Reports API credentials from the renderer.
- **Impact:** Third-party rights credentials and tokens are exposed to browser/Electron renderer memory, devtools, and client network paths. This bypasses the backend-only API boundary used elsewhere in the app and makes CORS/provider behavior part of the user-facing control path.
- **Fix Direction:** Move ASCAP, BMI, SoundExchange, and Music Reports calls behind Firebase/Cloud Run functions that verify Firebase Auth/App Check, load provider credentials server-side, and return typed honest statuses. Renderer should submit only metadata and receive `registered`, `pending_credentials`, `manual_required`, or `failed` results.
- **Verified?:** Static verified by source read; provider calls were not executed.

### ISSUE-656 | OPEN | HIGH | PRO Setlist Submission Writes Fake Submitted State

- **Module:** Renderer / rights / live setlist submission
- **GitHub:** https://github.com/indii-music-founder/indii-music-founder/issues/215
- **Evidence:** `packages/renderer/src/services/rights/PRORightsService.ts:564-571` logs a PRO gateway submission and computes an ASCAP/BMI gateway URL, but never calls `fetch`.
- **Evidence:** `packages/renderer/src/services/rights/PRORightsService.ts:571-588` hardcodes `const mockResponseOk = true`, generates `SUB-${targetPRO}-...`, writes `submissionStatus: 'Submitted'` to Firestore, and returns `{ success: true }`.
- **Impact:** The app can tell an artist that an ASCAP/BMI live setlist was submitted when no external submission occurred. This directly affects royalty collection and business/legal trust.
- **Fix Direction:** Remove the fake success path. Either route setlist submission through a real secured backend integration or return an honest `manual_required` / `unavailable` result without updating Firestore to `Submitted`.
- **Verified?:** Static verified by source read; no live PRO submission attempted.

### ISSUE-657 | OPEN | HIGH | Royalty Report Ingestion Can Duplicate Payouts

- **Module:** Renderer / finance / royalty ingestion
- **GitHub:** https://github.com/indii-music-founder/indii-music-founder/issues/216
- **Evidence:** `packages/renderer/src/services/finance/RoyaltyService.ts:40-65` starts `ingestRevenueReport(reportId, ...)` and opens per-release transactions without checking whether the report was already processed.
- **Evidence:** `packages/renderer/src/services/finance/RoyaltyService.ts:82-103` deducts recoupment, calculates payouts, and writes each payout to `doc(collection(db, this.PAYOUTS_COLLECTION))`, generating fresh payout document IDs on every run.
- **Evidence:** `packages/renderer/src/services/finance/RoyaltyService.ts:108-113` writes the new recoupment balance, so duplicate ingestion can also mutate recoupment twice.
- **Evidence:** `packages/renderer/src/services/finance/RoyaltyService.test.ts:82-89` covers one successful ingestion only; no retry/idempotency regression is asserted.
- **Impact:** A user retry, importer retry, or duplicate upload of the same DSP report can create duplicate pending payout records and drift recoupment/accounting balances.
- **Fix Direction:** Add a processed-report claim or deterministic payout IDs based on `reportId`, source transaction, ISRC, and payee. Claim/check the report inside the transaction before creating payouts, and test duplicate ingestion plus partial retry behavior.
- **Verified?:** Static verified by source read; Firestore mutation was not executed.

### ISSUE-658 | OPEN | HIGH | Distributor Adapters Report Pending Review Without Confirmed Delivery

- **Module:** Renderer / distribution / distributor adapters
- **GitHub:** https://github.com/indii-music-founder/indii-music-founder/issues/217
- **Evidence:** `packages/renderer/src/services/distribution/adapters/TuneCoreAdapter.ts:87-139` attempts TuneCore API delivery, logs non-OK or fetch failure, then falls back to `success: true`, `status: 'pending_review'`, and a synthetic `TC-...` distributor release id.
- **Evidence:** `packages/renderer/src/services/distribution/adapters/BelieveAdapter.ts:122-173`, `OnerpmAdapter.ts:116-164`, and `UnitedMastersAdapter.ts:117-164` use the same success fallback after API failure or missing API delivery confirmation.
- **Evidence:** `packages/renderer/src/services/distribution/adapters/SymphonicAdapter.ts:73-101` returns `success: true` from the SFTP branch even if ERN generation, staging, or upload never ran because nested success checks were false.
- **Evidence:** `packages/renderer/src/services/distribution/adapters/DistributionAdapters.test.ts:68` mocks `fetch` to reject, while `:201-209` still expects TuneCore `createRelease()` to succeed as simulated API delivery.
- **Impact:** Artists can be told a release is in DSP review with a distributor release id when the app only generated ERN/manual-ready metadata or failed to contact the provider.
- **Fix Direction:** Reserve `success: true` for confirmed DSP API acceptance or SFTP upload. Return explicit `manual_required`, `delivery_unavailable`, or `ready_for_manual_submission` states for ERN-only/manual fallbacks, and update tests so rejected API calls do not pass as delivery success.
- **Verified?:** Static verified by source and test reads; no live distributor submission attempted.

### ISSUE-659 | OPEN | HIGH | Distributor Takedown Adapters Fabricate Requested State

- **Module:** Renderer / distribution / takedowns
- **GitHub:** https://github.com/indii-music-founder/indii-music-founder/issues/218
- **Evidence:** `packages/renderer/src/services/distribution/adapters/SymphonicAdapter.ts:153-163` logs a takedown and returns `success: true`, `status: 'takedown_requested'`, but performs no provider call.
- **Evidence:** `packages/renderer/src/services/distribution/adapters/TuneCoreAdapter.ts:213-218`, `BelieveAdapter.ts:223-225`, `OnerpmAdapter.ts:214-216`, and `UnitedMastersAdapter.ts:207-209` return the same successful takedown state without external calls.
- **Evidence:** `packages/renderer/src/services/distribution/DistributorService.ts` reports `canTakedown: true` for every registered adapter instead of deriving it from real adapter support.
- **Impact:** Artists can believe a DSP takedown was requested while no platform removal request was transmitted, leaving unwanted or legally risky content live.
- **Fix Direction:** Implement real provider takedown integrations or return honest `manual_required` / `unsupported` results. Make connection capability reporting reflect actual adapter support and add tests proving no adapter reports `takedown_requested` without an accepted external handoff.
- **Verified?:** Static verified by source read; no live takedown attempted.

### ISSUE-660 | OPEN | HIGH | Takedown Request Marks Release Before Provider Notification

- **Module:** Firebase / distribution takedown records
- **GitHub:** https://github.com/indii-music-founder/indii-music-founder/issues/219
- **Evidence:** `packages/firebase/src/functions/distribution/distributionRecords.ts:281-329` creates internal takedown request records and immediately writes `status: "takedown_requested"` to the release document in the same transaction.
- **Evidence:** `packages/renderer/src/services/agent/tools/DistributionTools.ts:770-788` expects a `processReleaseTakedown` callable to notify distributors after the internal request is created.
- **Evidence:** `packages/firebase/src/index.ts:94-101` exports the request/create/update record callables, but not `processReleaseTakedown`; `rg` finds no implementation under `packages/firebase/src`.
- **Impact:** The app can mark a release as takedown-requested before any distributor has accepted or even received a removal request.
- **Fix Direction:** Keep request-only state honest (`takedown_pending_notification`, `manual_required`, etc.) until a real provider notification worker/callable succeeds. Implement `processReleaseTakedown` or remove the renderer call and add tests for request-only versus provider-notified state transitions.
- **Verified?:** Static verified by backend export/source search; no live takedown attempted.

### ISSUE-661 | OPEN | HIGH | Sync Licensing Compiler Marks Rights Cleared Without Evidence

- **Module:** Renderer / licensing sync harness
- **GitHub:** https://github.com/indii-music-founder/indii-music-founder/issues/220
- **Evidence:** `packages/renderer/src/services/licensing/LicensingSyncCompiler.ts:104-146` initializes `rightsClearanceStatus` as `cleared`, only changes it to `blocked` when `hasUnClearedSamples` is true, and generates a pitch package when readiness is high.
- **Evidence:** `packages/renderer/src/services/licensing/LicensingSyncCompiler.ts:135-140` emits a Rights Clearance score of 100 with rationale `All rights cleared.` for that default state.
- **Evidence:** `packages/renderer/src/services/licensing/SyncLicensingClearanceService.ts:165-193` contains clearance-document status logic, but the compiler does not consume it.
- **Evidence:** `packages/renderer/src/services/licensing/LicensingSyncCompiler.test.ts:47-65` asserts a track with `hasUnClearedSamples: false` is `cleared` and `pitchPackageGenerated: true`.
- **Impact:** The app can tell artists or agents sync rights are cleared and generate pitch material without approved clearance docs or provider/legal review.
- **Fix Direction:** Default unknown clearance to `pending`; require approved clearance documents or provider/legal evidence before outputting `cleared`. Gate pitch-package generation on verified clearance or clearly mark it draft/manual-review-only.
- **Verified?:** Static verified by source and test reads; no runtime harness execution performed.

## Small Game Findings

### ISSUE-654 | OPEN | MEDIUM | Workspace Snapshot Rehydrate Mutates Zustand State Directly

- **Module:** Renderer / workspace sync / Zustand store
- **Evidence:** `packages/renderer/src/hooks/useWorkspaceSync.ts:100-102` calls `applyWorkspaceSnapshot(snapshot)` after the user accepts a newer cloud snapshot.
- **Evidence:** `packages/renderer/src/core/store/index.ts:195-223` implements `applyWorkspaceSnapshot` by assigning directly to the object returned from `useStore.getState()` (`state.boardroomMessages = ...`, `state.currentModule = ...`, `state.notes = ...`, etc.).
- **Impact:** Direct mutation bypasses Zustand's `setState` notification path. The underlying object may change, but React subscribers, persistence middleware, and sync subscribers are not guaranteed to see a state update, so cross-device restore can silently fail or only partially update the UI.
- **Fix Direction:** Build a patch object and call `useStore.setState(patch, false)` for root-store fields; use the existing living-plan slice setters for plan state. Add a regression test that subscribes to `useStore`, calls `applyWorkspaceSnapshot`, and asserts subscribers are notified and selected fields update.
- **Verified?:** Static verified by source read; no runtime sync test executed.

### ISSUE-662 | OPEN | MEDIUM | Creative Video Callable Contract Mismatch

- **Module:** Renderer/Firebase / creative video and avatar jobs
- **Evidence:** `packages/renderer/src/modules/creative/video/VideoWorkflow.tsx:758-770` calls Firebase callable `cancelVideoJob` and reports cancellation only after that callable resolves.
- **Evidence:** `packages/firebase/src/functions/creative/gateway.ts:1371-1395` implements `cancelVideoJob`, but `packages/firebase/src/index.ts:29-30` only exports `generateImageV3`, `generateVideoV3`, `generateOmniRemixV3`, `generateAudioV3`, and `videoJobFirestoreOrchestrator` from the creative gateway/orchestrator, so `cancelVideoJob` is not deployed from the root functions entry.
- **Evidence:** `packages/renderer/src/services/video/AvatarGenerationService.ts:53-60` calls `dispatchAvatarJob`, and `:87-92` calls `getAvatarJobStatus`; `rg` finds no backend implementations or exports for either callable under `packages/firebase/src`.
- **Impact:** Video cancellation can fail at runtime with a missing callable even though backend code exists, and avatar lip-sync generation/status cannot work through the declared service path.
- **Fix Direction:** Export `cancelVideoJob` from the Firebase root entry, implement/export `dispatchAvatarJob` and `getAvatarJobStatus` or remove/disable the avatar feature path behind an honest unavailable state. Add a callable-contract test that compares static renderer callable names against Firebase root exports.
- **Verified?:** Static verified by source/export search; no live callable invocation performed.

### ISSUE-663 | OPEN | MEDIUM | Distribution Automation Workers Are Referenced But Missing

- **Module:** Renderer/Firebase / distribution agent tools
- **Evidence:** `packages/renderer/src/services/agent/tools/DistributionTools.ts:485-528` creates a `video_releases` record with `status: 'QUEUED'`, then calls missing callable `distributeVideoToDSP`; `rg` finds no backend implementation or export under `packages/firebase/src`.
- **Evidence:** `packages/renderer/src/services/agent/tools/DistributionTools.ts:621-708` creates an SFTP ingestion record, then calls missing callable `sftpDeliverRelease` for server-side SFTP delivery; `rg` finds no backend implementation or export under `packages/firebase/src`.
- **Impact:** Premium video DSP distribution and server-side SFTP delivery cannot execute through the intended Cloud Function workers. They currently degrade to queued/manual states, which is honest but leaves automation incomplete and records stuck until a separate manual process exists.
- **Fix Direction:** Implement/export `distributeVideoToDSP` and `sftpDeliverRelease`, or remove the callable attempts and present these paths as manual-only. Add callable-contract coverage so renderer tools cannot reference undeployed worker names.
- **Verified?:** Static verified by source/export search; no live callable invocation performed.

### ISSUE-664 | OPEN | MEDIUM | Fan Enrichment Fabricates Scores Without Provider Credentials

- **Module:** Firebase / marketing fan enrichment
- **Evidence:** `packages/firebase/src/index.ts:1659` logs that the Clearbit API key is missing and `packages/firebase/src/index.ts:1694` logs that the Apollo API key is missing, but neither path fails closed.
- **Evidence:** `packages/firebase/src/index.ts:1698-1706` falls back to `providerUsed = 'mock'` and returns each fan with deterministic `enrichmentScore: email.length % 50 + 40` when `provider` is `clearbit`, `apollo`, or `mock`.
- **Evidence:** `packages/renderer/src/services/marketing/FanEnrichmentService.ts:79-108` calls `enrichFanData` and pushes returned `results` into `EnrichedFanData[]` without distinguishing mock fallback from real provider enrichment.
- **Impact:** Artists can make marketing decisions from fabricated enrichment scores when third-party enrichment is not configured. Older ledger entries marked this mock as fixed, but the current source still contains the credential-missing mock fallback.
- **Fix Direction:** Remove the mock fallback for missing Clearbit/Apollo credentials. Return a typed unavailable/configuration response or `HttpsError('failed-precondition', ...)`, and test missing-secret failure plus real provider pass-through.
- **Verified?:** Static verified by source read; no live Clearbit/Apollo calls performed.

### ISSUE-665 | OPEN | HIGH | SMS And Email Panels Fabricate Delivered Confirmations

- **Module:** Marketing / SMS and email campaign UI
- **GitHub:** https://github.com/indii-music-founder/indii-music-founder/issues/221
- **Evidence:** `packages/renderer/src/modules/marketing/components/SMSMarketingPanel.tsx:32-49` marks Twilio sender verification and send completion via `setTimeout`, not provider confirmation.
- **Evidence:** `packages/renderer/src/modules/marketing/components/SMSMarketingPanel.tsx:156-190` renders `Send Blast to ...`, `SMS Blast Sent`, and `Delivered ... via Twilio` from local state and hardcoded segment sizes.
- **Evidence:** `packages/renderer/src/modules/marketing/components/EmailMarketingPanel.tsx:44-50` marks send/schedule completion via `setTimeout`.
- **Evidence:** `packages/renderer/src/modules/marketing/components/EmailMarketingPanel.tsx:161-199` renders `Email Sent Successfully` / `Email Scheduled` and `Delivered ... via Mailchimp/Klaviyo` without calling `EmailMarketingService` or a backend function.
- **Impact:** Users can believe SMS/email campaigns were delivered to thousands of fans when no external provider accepted, scheduled, or sent anything.
- **Fix Direction:** Wire panels to real provider-backed services, remove fake timers and hardcoded recipient counts, disable or mark unavailable when providers are not configured, and test provider-unavailable/provider-confirmed states.
- **Verified?:** Static verified by source read; no live Twilio/Mailchimp/Klaviyo calls performed.

### ISSUE-666 | OPEN | HIGH | Multi-Platform Poster Marks All Platforms Posted After One Dispatch

- **Module:** Marketing / social auto-poster
- **GitHub:** https://github.com/indii-music-founder/indii-music-founder/issues/222
- **Evidence:** `packages/renderer/src/modules/marketing/components/MultiPlatformPoster.tsx:78-95` stores scheduled posts only in component-local React state.
- **Evidence:** `packages/renderer/src/modules/marketing/components/MultiPlatformPoster.tsx:102-108` posts only `post.platforms[0]`.
- **Evidence:** `packages/renderer/src/modules/marketing/components/MultiPlatformPoster.tsx:110-111` toasts all selected platforms and marks the whole post `posted` after that one platform call resolves.
- **Evidence:** `packages/firebase/src/lib/marketing.ts:56-65` normalizes only `twitter`, `instagram`/`meta_reels`, and `tiktok`, while the UI offers YouTube Shorts; `packages/firebase/src/lib/marketing.ts:143-173` queues only one `scheduledPosts` document per callable invocation.
- **Impact:** A user can select TikTok, YouTube Shorts, and IG Reels, then see all platforms reported as dispatched/posted while only one platform was queued and YouTube cannot be queued through this path.
- **Fix Direction:** Persist scheduled posts through the backend, queue each selected platform independently, align YouTube platform IDs across UI/callable/worker, and present per-platform success/failure.
- **Verified?:** Static verified by source read; no live social posting attempted.

### ISSUE-667 | OPEN | MEDIUM | Marketing Provider Services Reference Missing Callables And Fake Fallbacks

- **Module:** Marketing / provider service contracts
- **Evidence:** `packages/renderer/src/services/marketing/SMSMarketingService.ts:38-85` calls `sendSMSBlast` and `getSMSDeliveryStatus`; `rg` finds no backend implementation/export under `packages/firebase/src`. Failure paths return local queued/status values (`false` from dispatch, `pending` for status).
- **Evidence:** `packages/renderer/src/services/marketing/EmailMarketingService.ts:39-121` calls `syncEmailList`, `deployEmailCampaign`, and `getEmailCampaignStats`; `rg` finds no backend implementation/export under `packages/firebase/src`. Stats fallback returns zero-filled metrics.
- **Evidence:** `packages/renderer/src/services/marketing/SocialAutoPosterService.ts:95-128` has `revokePost()` returning `true` without deleting anything and `getPostInsights()` calling missing `getSocialPostInsights`, then returning zero-filled metrics.
- **Impact:** Even if the UI were wired to the services, provider delivery/status/analytics paths would still depend on undeployed callables or fake local fallbacks.
- **Fix Direction:** Implement/export provider callables or make these methods fail honestly with typed unavailable states. Add callable-contract tests and service tests for unavailable provider behavior.
- **Verified?:** Static verified by source/export search; no live provider calls performed.

### ISSUE-668 | OPEN | MEDIUM | Influencer Bounty Tracking And Payout Paths Are Not Wired End-To-End

- **Module:** Marketing / influencer bounty board
- **Evidence:** `packages/firebase/src/lib/marketing.ts:202-214` writes created bounties to the `influencerBounties` collection and returns a backend-generated `link`.
- **Evidence:** `packages/renderer/src/modules/marketing/components/InfluencerBountyBoard.tsx:41-74` keeps all board and leaderboard records in local React state only.
- **Evidence:** `packages/renderer/src/modules/marketing/components/InfluencerBountyBoard.tsx:80-113` creates a backend bounty, then stores a local copy and copies `https://indii.vip/ref/${refCode}` instead of the returned backend `link`.
- **Evidence:** `packages/renderer/src/services/marketing/InfluencerBountyService.ts:81-99` leaves `trackEvent()` as a no-op and returns `pyt_${Date.now()}` from `initiatePayout()` without a Stripe transfer.
- **Evidence:** `packages/renderer/src/services/marketing/InfluencerBountyService.ts:105-149` queries `bountyLinks`, but the backend creation callable writes `influencerBounties`.
- **Impact:** Bounty records disappear from the board on reload, copied referral URLs can disagree with configured backend URLs, leaderboards read the wrong collection, and any service-level payout call can report a payout ID without moving money.
- **Fix Direction:** Use the same persisted backend collection for board, leaderboard, tracking, and payouts. Copy the returned backend link. Implement tracking/payout workers or make those paths unavailable until they exist.
- **Verified?:** Static verified by source read; no live referral/payout flow executed.

The email provider mutation flow was also traced through provider, service facade, and Inbox UI call sites during the Small Game pass.

## False Leads Discarded

- `dangerouslySetInnerHTML`: only hit was `AgentCanvasPanel.tsx`, which sanitizes through `DOMPurify.sanitize(...)`.
- Hardcoded secret scan: hits were `ModelArmor` secret-detection regexes and string probes, not real committed credentials.
- `process.env` in renderer: hits were test/runtime guards for `VITEST` and `NODE_ENV`, or logger fallbacks guarded behind `import.meta.env`.
- API integrity: `node scripts/verify-api-system-integrity.js` passed with no ghost tests or legacy browser AI dependencies.
- Ghost project IDs: no `indii-v-1-1` or `223837784072` hits in scanned code paths.
- `Math.random()` / `Date.now()` render scan: new actionable item not found. `ScreenwriterDashboard` random scene ID is already tracked as `ISSUE-456`; most timestamp hits are event handlers, filename/id creation, display formatting, or refs.
- Loading gates: several loading returns exist, but this pass did not prove a missing error/timeout path strongly enough to log a new issue.
- Locale scan: broad output was mostly explicit `en-US` formatting or display-only surfaces; no new DDEX/invoice/legal date bug was proven.
- Firestore update scan: several direct updates exist, but the inspected scheduled-post delivery path already uses a transaction to claim work before mutation. No new race was proven from the sampled files.
- Store non-serializable state scan: `Map`, `File[]`, `AbortController`, and approval resolver values exist, but root-store persistence uses `partialize` and the standalone project slice persists only `selectedProjectId`; no storage-corruption issue was proven from those alone.
- `useStore` selector scan: found a few multi-property selectors without `useShallow`, but no correctness bug was proven in this pass. Treat as possible performance cleanup, not a hunter issue.
- Historical ledger note: `ISSUE-052` says PRO live setlist submission was fixed by extending `PRORightsService`, but the current implementation still contains a fabricated success path. This audit logs the current contradiction as `ISSUE-656` instead of rewriting old history.
- Printful POD callable export check: the renderer dynamically calls `pod_${name}` from `PrintOnDemandService`, and `packages/firebase/src/index.ts:1728-1743` exports the `pod_printful*` functions, so no Printful callable export issue was logged.
- Fan enrichment region check: `FanEnrichmentService` imports `functionsWest1`, but `packages/renderer/src/services/firebase.ts:273-277` aliases `functionsWest1 = functions` for the central functions client, matching `enrichFanData` in `us-central1`; no region-mismatch issue was logged.

## Verification Commands

- `node scripts/verify-api-system-integrity.js` -> passed.
- `rg -n "indii-v-1-1|223837784072" packages scripts execution load-tests --glob '*.{ts,js,sh,py}' --glob '!**/node_modules/**'` -> no matches.
- Static callable comparison found missing `cancelVideoJob`, `distributeVideoToDSP`, `processReleaseTakedown`, and `sftpDeliverRelease`; those are covered by `ISSUE-662`, `ISSUE-663`, and `ISSUE-660`. It also produced false positives for docs/test strings (`fn`, `httpsCallable`) and a parser miss for `createStripePaymentLinks`, which is exported from `packages/firebase/src/index.ts:1731`.
- Static source reads listed in each finding.

## GitHub Issues

- High severity findings require GitHub issue handoff per `/hunter`.
- ISSUE-655: https://github.com/indii-music-founder/indii-music-founder/issues/214
- ISSUE-656: https://github.com/indii-music-founder/indii-music-founder/issues/215
- ISSUE-657: https://github.com/indii-music-founder/indii-music-founder/issues/216
- ISSUE-658: https://github.com/indii-music-founder/indii-music-founder/issues/217
- ISSUE-659: https://github.com/indii-music-founder/indii-music-founder/issues/218
- ISSUE-660: https://github.com/indii-music-founder/indii-music-founder/issues/219
- ISSUE-661: https://github.com/indii-music-founder/indii-music-founder/issues/220
- ISSUE-665: https://github.com/indii-music-founder/indii-music-founder/issues/221
- ISSUE-666: https://github.com/indii-music-founder/indii-music-founder/issues/222
