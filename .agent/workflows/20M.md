---
description: Run the adjustable 20M valuation, investor-room, pricing, and acquisition system. Defaults to $20M; accepts 50M, 100M, 1B, or custom targets.
---

# /20M — Adjustable Valuation & Acquisition North Star

Read `.agent/skills/20M/SKILL.md` completely and follow every routed reference.

- `/20M` defaults to $20M target enterprise value.
- `/20M 50M`, `/20M 100M`, `/20M 1B`, or several targets reruns the same evidence model.
- `/20M room` prepares and operates live investor Q&A.

Preserve exact user scenarios. Freeze the current state, research current sources, separate current/remediated/traction value and founder cash from ARR, run deterministic math, validate the report, and save durable outputs under `.agent/reports/20M/YYYY-MM-DD-TARGET/`.

In room mode put “What does it do?” and “Why do I need it?” first, each with explicit 20-second and two-minute answers. Use one evidence baseline across all apps and never rely on hidden chat state.

> **Mainline delivery gate:** Before any code, git, CI, push, or optional branch action, read and obey [`branch-safety.md`](branch-safety.md). Direct-to-`main` is mandatory unless the user explicitly requests a branch.
